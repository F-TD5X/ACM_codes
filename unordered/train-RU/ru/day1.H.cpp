#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
ll po[1001];
ll hashCode(char *s)
{
    ll ret = 0;
    ll len = strlen(s);
    for(ll i =0;i<len;i++)
    {

    }
}

int main()
{
    for(int i =0;i<1000;i++)
    {
        po[i] = pow(31,i);
        cout << po[i] <<endl;
    }
}
